using UnityEngine;

namespace Dark.Modularity
{
    public class ModuleSocket
    {
        public readonly Vector2 LocalPosition;
        public ModuleSocket(Vector2 localPosition)
        {
            LocalPosition = localPosition;
        }
    }
}
